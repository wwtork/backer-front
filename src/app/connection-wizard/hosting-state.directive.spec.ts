import { HostingStateDirective } from './hosting-state.directive';

describe('HostingStateDirective', () => {
  it('should create an instance', () => {
    const directive = new HostingStateDirective();
    expect(directive).toBeTruthy();
  });
});
