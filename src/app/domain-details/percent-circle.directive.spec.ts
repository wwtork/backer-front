import { PercentCircleDirective } from './percent-circle.directive';

describe('PercentCircleDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentCircleDirective();
    expect(directive).toBeTruthy();
  });
});
